<h1><%= @patient.name %></h1>

<ul>
  <% @patient.appointments.each do |appt| %>
    <li>Appointment: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")%>
      Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
  <% end %>
</ul>
