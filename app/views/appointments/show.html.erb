
<h1>Appointment: <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></h1>
<h2> With Doctor: <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></h2>
<h2>For Patient: <%= link_to @appointment.patient.name, patient_path(@appointment.patient)%></h2>
